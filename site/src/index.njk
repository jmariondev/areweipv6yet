---
layout: base.njk
title: Are We IPv6 Yet? - Track IPv6 Adoption Across Popular Services
description: Find out if Google, Netflix, GitHub, and 60+ other popular services support IPv6. Community-driven tracker with automated DNS verification and detailed test results.
---
{%- set sortedServices = serviceList | sortServices -%}
{%- set statsFull = serviceList | countByStatus('full') -%}
{%- set statsPartial = serviceList | countByStatus('partial') -%}
{%- set statsNone = serviceList | countByStatus('none') -%}
{%- set statsUnknown = serviceList | countByStatus('unknown') -%}
{%- set statsTotal = serviceList | length -%}

<section class="summary">
  <div class="progress-container">
    <h2 class="progress-title">IPv6 Adoption Progress</h2>
    <p class="progress-note">(among the services we track!)</p>
    <div class="progress-bar">
      {%- set percentFull = ((statsFull / statsTotal) * 100) | round -%}
      {%- set percentPartial = ((statsPartial / statsTotal) * 100) | round -%}
      {%- set percentNone = ((statsNone / statsTotal) * 100) | round -%}
      {%- set percentUnknown = ((statsUnknown / statsTotal) * 100) | round -%}
      <div class="progress-segment progress-full" style="width: {{ percentFull }}%" title="Full Support: {{ statsFull }} services ({{ percentFull }}%)">
        <span class="progress-label">{{ statsFull }}</span>
      </div>
      <div class="progress-segment progress-partial" style="width: {{ percentPartial }}%" title="Partial Support: {{ statsPartial }} services ({{ percentPartial }}%)">
        <span class="progress-label">{{ statsPartial }}</span>
      </div>
      <div class="progress-segment progress-none" style="width: {{ percentNone }}%" title="No Support: {{ statsNone }} services ({{ percentNone }}%)">
        <span class="progress-label">{{ statsNone }}</span>
      </div>
      {%- if statsUnknown > 0 -%}
      <div class="progress-segment progress-unknown" style="width: {{ percentUnknown }}%" title="Unknown: {{ statsUnknown }} services ({{ percentUnknown }}%)">
        <span class="progress-label">{{ statsUnknown }}</span>
      </div>
      {%- endif -%}
    </div>
    <div class="progress-legend">
      <div class="legend-item">
        <span class="legend-color legend-full"></span>
        <span>Full ({{ percentFull }}%)</span>
      </div>
      <div class="legend-item">
        <span class="legend-color legend-partial"></span>
        <span>Partial ({{ percentPartial }}%)</span>
      </div>
      <div class="legend-item">
        <span class="legend-color legend-none"></span>
        <span>None ({{ percentNone }}%)</span>
      </div>
      {%- if statsUnknown > 0 -%}
      <div class="legend-item">
        <span class="legend-color legend-unknown"></span>
        <span>Unknown ({{ percentUnknown }}%)</span>
      </div>
      {%- endif -%}
    </div>
  </div>
  <div class="stats">
    <div class="stat stat-full">
      <span class="stat-number">{{ statsFull }}</span>
      <span class="stat-label">Full Support</span>
    </div>
    <div class="stat stat-partial">
      <span class="stat-number">{{ statsPartial }}</span>
      <span class="stat-label">Partial Support</span>
    </div>
    <div class="stat stat-none">
      <span class="stat-number">{{ statsNone }}</span>
      <span class="stat-label">No Support</span>
    </div>
    {%- if statsUnknown > 0 -%}
    <div class="stat stat-unknown">
      <span class="stat-number">{{ statsUnknown }}</span>
      <span class="stat-label">Unknown</span>
    </div>
    {%- endif -%}
  </div>
</section>

<section class="filters">
  <input type="search" id="search" placeholder="Search services...">
  <div class="filter-buttons">
    <button class="filter-btn active" data-filter="all">All</button>
    <button class="filter-btn" data-filter="full">✅ Full</button>
    <button class="filter-btn" data-filter="partial">🟨 Partial</button>
    <button class="filter-btn" data-filter="none">❌ None</button>
    {%- if statsUnknown > 0 -%}
    <button class="filter-btn" data-filter="unknown">❓ Unknown</button>
    {%- endif -%}
  </div>
</section>

<section class="services" id="services">
  {%- for service in sortedServices -%}
  <div class="service-card status-{{ service.ipv6.status }}" data-service-id="{{ service.id }}">
    <div class="service-header">
      <h3>{{ service.name }}</h3>
      <div class="status-badge">
        <span class="status-emoji">{{ service.ipv6.status | statusEmoji }}</span>
        <span class="status-text">{{ service.ipv6.status }}</span>
      </div>
    </div>
    <p class="service-url">{{ service.url }}</p>
    {%- if service.description -%}
    <p class="service-description">{{ service.description }}</p>
    {%- endif -%}
    
    {%- if service.ipv6.notes -%}
    <p class="service-notes">{{ service.ipv6.notes }}</p>
    {%- endif -%}
    
    {%- set visibleTests = service.ipv6.tests | visibleTests -%}
    {%- if visibleTests | length > 0 -%}
    <div class="service-tests">
      {%- for test in visibleTests -%}
      {%- set testClass = 'test-pass' if test.result == true else ('test-fail' if test.result == false else 'test-unknown') -%}
      <div class="test-item {{ testClass }}">
        <span class="test-icon">{{ test.result | testIcon }}</span>
        <span class="test-label">{{ test.name }}</span>
      </div>
      {%- endfor -%}
    </div>
    {%- endif -%}
    
    {%- if service.ipv6.last_checked -%}
    <p class="last-checked">Last checked: {{ service.ipv6.last_checked | shortDate }}</p>
    {%- endif -%}
  </div>
  {%- endfor -%}
</section>

<section class="contribute-section">
  <div class="contribute-card">
    <h2>🚀 Help Track IPv6 Adoption!</h2>
    <p>This project relies on community contributions to stay accurate and up-to-date.</p>
    <div class="contribute-actions">
      <div class="contribute-item">
        <h3>📝 Add Missing Services</h3>
        <p>Know a service that's not listed? Add it to our database!</p>
      </div>
      <div class="contribute-item">
        <h3>🔄 Update Status</h3>
        <p>Found incorrect IPv6 status? Help us fix it!</p>
      </div>
      <div class="contribute-item">
        <h3>🐛 Report Issues</h3>
        <p>Spotted a problem? Let us know!</p>
      </div>
    </div>
    <a href="https://github.com/jmariondev/areweipv6yet" class="contribute-btn">
      <svg class="github-icon" viewBox="0 0 24 24" width="20" height="20">
        <path fill="currentColor" d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
      </svg>
      Contribute on GitHub
    </a>
    <p class="contribute-note">All contributions are welcome! Check our README for guidelines.</p>
  </div>
</section>